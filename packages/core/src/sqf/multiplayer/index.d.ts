
/**
 * Returns true if the machine executing the command is the
 * server in a multiplayer game or is running single player.
 * It will return true for both dedicated and player-hosted
 * server.
 *
 * @since 0.50
 *
 * @see {@link https://community.bistudio.com/wiki/isServer isServer}
 */
export declare const isServer: boolean
